/*

const names = ["John", "Pete", "Mary"];

const sayHello = () => {
  for (let i = 0; i < names.length; i++) {
    setTimeout(
      () => {
        console.log(`Hello, ${names[i]}!`);
      },
      500 * (i + 1),
    );
  }
};

sayHello();

*/
/*
import names from "./2-names.js";
import sayHi from "./3-utils.js";
sayHi(names.john);
sayHi(names.pete);
sayHi(names.mary);
*/

//console.log(global);
